name: 'nocalhost server cli'
description: 'nocalhost server cli'
author: 'zj'
branding:
  icon: 'box'
  color: 'gray-dark'
inputs:
  host:
    required: true
    description: 'host'
  email:
    required: true
    description: 'email'
  password:
    required: true
    description: 'password'
  action:
    required: true
    description: 'action'
  parameters:
    required: false
    description: 'parameters'
runs:
  using: 'node16'
  main: 'dist/index.js'
  post: 'dist/post.js'
  post-if: 'success()'
